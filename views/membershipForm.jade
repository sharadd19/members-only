extends layout

block content
  
  div.wrapper
    h1 #{title}
    a(href="/user") Back
    if membership 
      h3 You are already a member! 
      
    else
    
      form(method='POST' action="/user/membership")
        div.form-group
          div.secret
            label(for="secret") #[strong Secret]: 
            input#secret.form-control(type='text', name='secret' required)  
          div.button-group
            button(type="submit" class="button") Get Membership

      if error
        h3 #{error.message}
    


